# Theater Mode Environment
# Copy to .env.theater and customize
#
# Usage: npm run dev:theater

# WebSocket server for multiplayer (your Fly.io deployment)
VITE_WS_URL=wss://protoverse-yourMovie.fly.dev:8765

# Convex HTTP endpoint (session discovery + AI proxy)
VITE_CONVEX_HTTP_URL=https://your-deployment.convex.site

# CDN for world assets
VITE_CDN_URL=https://your-bucket.storage.dev

# Public URL for join links
VITE_PROTOVERSE_URL=https://your-app.netlify.app
